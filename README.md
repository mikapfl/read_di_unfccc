Library to load data from the UNFCCC API available at https://di.unfccc.int.

Uses the `treelib`, `requests`, and `pandas` libraries.

Check out the `example.ipynb` notebook for a simple usage example.
